import React,{useCallback,useEffect,useState} from 'react';import * as FiIcons from 'react-icons/fi';import SafeIcon from '../common/SafeIcon';import BookingTypeForm from './BookingTypeForm';import GoogleCalendarConnection from './GoogleCalendarConnection';import {archiveBookingType,listBookingTypes,saveBookingType,setBookingTypeEnabled} from '../services/bookingTypeService';import './BookingTypes.css';const {FiCalendar,FiClock,FiEdit2,FiLink,FiMapPin,FiPlus,FiRefreshCw,FiTrash2}=FiIcons;function BookingTypes(){const [bookingTypes,setBookingTypes]=useState([]);const [editor,setEditor]=useState(null);const [notice,setNotice]=useState('');const [loading,setLoading]=useState(true);const load=useCallback(async()=>{try{setBookingTypes(await listBookingTypes());}catch(error){setNotice(error.message);}finally{setLoading(false);}},[]);useEffect(()=>{load();},[load]);const save=async(values,existing)=>{try{const saved=await saveBookingType(values,existing);setBookingTypes((current)=>existing?current.map((item)=>item.id===saved.id?saved:item):[saved,...current]);setEditor(null);setNotice(existing?'Booking type updated.':'Booking type created.');}catch(error){setNotice(error.message);}};const toggleEnabled=async(item)=>{try{const updated=await setBookingTypeEnabled(item.id,!item.is_enabled);setBookingTypes((current)=>current.map((row)=>row.id===updated.id?{...row,...updated}:row));setNotice(updated.is_enabled?`${item.booking_name} is enabled for booking.`:`${item.booking_name} is disabled for booking.`);}catch(error){setNotice(error.message);}};const archive=async(item)=>{if(!window.confirm(`Archive ${item.booking_name}?`))return;try{await archiveBookingType(item.id);setBookingTypes((current)=>current.filter((row)=>row.id!==item.id));setNotice('Booking type archived.');}catch(error){setNotice(error.message);}};const openCreateEditor=()=>{setNotice('');setEditor({bookingType:null});};return(<section className="booking-types"><header className="booking-types__header"><div><span className="dashboard-eyebrow">Booking catalogue</span><h2>Configure Bookings</h2><p>Design clear booking options with custom availability,professional details and client-ready instructions.</p></div><button className="dashboard-primary" type="button" onClick={openCreateEditor}><SafeIcon icon={FiPlus}/>Create booking type</button></header><GoogleCalendarConnection/>{editor&&<BookingTypeForm bookingType={editor.bookingType} onClose={()=>setEditor(null)} onSaved={save}/>} {notice&&<p className="booking-types__notice" role="status">{notice}</p>}{loading?<div className="booking-types__state"><SafeIcon icon={FiRefreshCw}/>Loading booking types…</div>:bookingTypes.length?<div className="booking-type-grid">{bookingTypes.map((item)=><BookingTypeCard item={item} key={item.id} onEdit={()=>setEditor({bookingType:item})} onArchive={()=>archive(item)} onToggle={()=>toggleEnabled(item)}/>)}</div>:<div className="booking-types__state">No booking types yet. Create one to begin.</div>}</section>);}function BookingTypeCard({item,onEdit,onArchive,onToggle}){const isEnabled=item.is_enabled!==false;const isFullDay=item.duration==='full-day';const modeLabel=item.booking_type==='offline'?'In person':'Online';const availabilityLabel=isFullDay?'Full-day dates':`${item.selected_time_slots?.length||0} time slots`;return(<article className="booking-type-card"><div className="booking-type-card__glow"/><div className="booking-type-card__top"><span className={isEnabled?'booking-type-badge booking-type-badge--active':'booking-type-badge booking-type-badge--muted'}><i/>{isEnabled?'Live booking':'Hidden booking'}</span><div className="booking-type-card__actions"><button type="button" onClick={onEdit} aria-label={`Edit ${item.booking_name}`}><SafeIcon icon={FiEdit2}/></button><button type="button" onClick={onArchive} aria-label={`Archive ${item.booking_name}`}><SafeIcon icon={FiTrash2}/></button></div></div><div className="booking-type-card__identity"><div className="booking-type-card__photo">{item.photo_url?<img src={item.photo_url} alt={`${item.booking_name} avatar`}/>:<SafeIcon icon={item.booking_type==='offline'?FiMapPin:FiLink}/>}</div><div className="booking-type-card__title"><span className="booking-type-card__eyebrow">{modeLabel} booking</span><h3>{item.booking_name}</h3><p>{item.professional_name||'Professional not specified'}<span> · </span>{item.professional_expertise||'Expertise not specified'}</p></div></div><div className="booking-type-card__summary"><div><span>Availability</span><strong><SafeIcon icon={isFullDay?FiCalendar:FiClock}/>{availabilityLabel}</strong></div><div><span>Duration</span><strong>{isFullDay?'Full day':`${item.duration} min`}</strong></div><div><span>Cost</span><strong>{item.show_cost?`₹${Number(item.cost_inr||0).toLocaleString('en-IN')}`:'Not shown'}</strong></div></div><div className="booking-type-card__footer"><span className="booking-type-card__days">{item.available_days?.length||0} days configured</span><button type="button" className={isEnabled?'booking-status-toggle booking-status-toggle--enabled':'booking-status-toggle booking-status-toggle--disabled'} onClick={onToggle} aria-pressed={isEnabled}><span className="booking-status-toggle__dot"/>{isEnabled?'Enabled':'Disabled'}</button></div></article>);}export default BookingTypes;