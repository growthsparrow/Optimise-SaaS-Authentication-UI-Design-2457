import React,{useEffect,useState} from 'react';import {Navigate,useLocation} from 'react-router-dom';import supabase from '../supabase/supabase';import {isSuperAdmin,isUserEnabled} from '../services/adminService';import {getTeamMemberSession} from '../services/teamMemberService';function ProtectedRoute({children}) {const [state,setState]=useState({loading:true,session:null,member:null,isAdmin:false,isEnabled:false});const location=useLocation();useEffect(()=> {let active=true;const checkAccess=async ()=> {const [{data: sessionData},member]=await Promise.all([supabase.auth.getSession(),Promise.resolve(getTeamMemberSession())]);if (!active) return;if (member) {setState({loading:false,session:null,member,isAdmin:false,isEnabled:true});return;} const session=sessionData.session;if (!session) {setState({loading:false,session:null,member:null,isAdmin:false,isEnabled:false});return;} let admin=false;let enabled=false;try {enabled=await isUserEnabled();if (enabled) {admin=await isSuperAdmin();}} catch {enabled=false;}if (active) {setState({loading:false,session,member:null,isAdmin:admin,isEnabled:enabled});}};checkAccess();const {data}=supabase.auth.onAuthStateChange((_event,session)=> {if (!active) return;if (!session) {setState({loading:false,session:null,member:getTeamMemberSession(),isAdmin:false,isEnabled:false});return;} Promise.all([isUserEnabled(),isSuperAdmin()]).then(([enabled,admin])=> {if (active) setState({loading:false,session,isAdmin:admin,isEnabled:enabled,member:null});}).catch(()=> {if (active) setState({loading:false,session,isAdmin:false,isEnabled:false,member:null});});});return ()=> {active=false;data.subscription.unsubscribe();};},[]);if (state.loading) {return ( <main className="dashboard-loading"> <div className="dashboard-loader" aria-label="Loading dashboard" /> </main> );}if (state.isAdmin) {return <Navigate to="/gsadmin/dashboard" replace />;}if (!state.session&&!state.member) {return <Navigate to="/" replace state={{from:location}} />;}if (!state.isEnabled && !state.member) {return <Navigate to="/" replace state={{disabled:true}} />;}return children;}export default ProtectedRoute;