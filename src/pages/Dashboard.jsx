import React,{useEffect,useState} from 'react';import * as FiIcons from 'react-icons/fi';import SafeIcon from '../common/SafeIcon';import DashboardSidebar from '../components/DashboardSidebar';import AppointmentOverview from '../components/AppointmentOverview';import AnalyticsReports from '../components/AnalyticsReports';import BookingPageManager from '../components/BookingPageManager';import ConsultationsManager from '../components/ConsultationsManager';import DoctorsManager from '../components/DoctorsManager';import LocationsManager from '../components/LocationsManager';import TeamMembersManager from '../components/TeamMembersManager';import ProfileManager from '../components/ProfileManager';import Footer from '../components/Footer';import {getCustomerId} from '../services/profileService';import {clearTeamMemberSession,getTeamMemberSession} from '../services/teamMemberService';import supabase from '../supabase/supabase';import './Dashboard.css';import './DashboardMotion.css';import './DashboardPolish.css';import './DashboardEditors.css';import './DashboardAppointmentsFirst.css';import './SidebarRefinements.css';import './DashboardVisibility.css';const {FiLogOut}=FiIcons;function Dashboard() {const [sidebarOpen,setSidebarOpen]=useState(false);const [activeItem,setActiveItem]=useState('Appointments Overview');const [profile,setProfile]=useState(null);const [customerId,setCustomerId]=useState('');const [member,setMember]=useState(getTeamMemberSession());const [signingOut,setSigningOut]=useState(false);useEffect(()=> {let active=true;if (member) {return ()=> {active=false;};} supabase.auth.getUser().then(async ({data})=> {if (!active) {return;} const currentUser=data.user;setProfile(currentUser);if (currentUser?.id) {try {setCustomerId(await getCustomerId(currentUser.id));} catch {setCustomerId('');}}});const handleProfileUpdated=(event)=> {const updates=event.detail || {};setProfile((current)=> current ? {...current,user_metadata:{...current.user_metadata,...updates}} : current);};window.addEventListener('profile-updated',handleProfileUpdated);return ()=> {active=false;window.removeEventListener('profile-updated',handleProfileUpdated);};},[member]);const businessName=profile?.user_metadata?.business_name?.trim() || 'your workspace';const handleSignOut=async ()=> {setSigningOut(true);if (member) {clearTeamMemberSession();window.location.assign('/');return;} await supabase.auth.signOut();};const selectItem=(item)=> {setActiveItem(item);setSidebarOpen(false);};const renderActiveSection=()=> {if ( member && !['Appointments Overview','Manage Booking Page'].includes(activeItem) ) {return <AppointmentOverview />;} if (activeItem==='Analytics and Reports') {return <AnalyticsReports />;} if (activeItem==='Manage Booking Page') {return <BookingPageManager businessName={businessName} />;} if (activeItem==='Manage Doctors') {return <DoctorsManager />;} if (activeItem==='Manage Consultations') {return <ConsultationsManager />;} if (activeItem==='Manage Locations') {return <LocationsManager />;} if (activeItem==='Manage Team Members') {return <TeamMembersManager />;} if (activeItem==='Profile Management') {return <ProfileManager />;} return <AppointmentOverview />;};return ( <main className="dashboard-page"><DashboardSidebar open={sidebarOpen} onClose={()=> setSidebarOpen((current)=> !current)} activeItem={activeItem} onSelect={selectItem} memberMode={Boolean(member)} /><section className="dashboard-content"><header className="dashboard-header"><div><span className="dashboard-eyebrow">{member ? 'Team member workspace' : 'Medical and Healthcare'}</span><h1>Hello,{member ? member.full_name : businessName}</h1><p>{member ? 'View appointments,download reports,and update patient statuses.' : `Here is what is happening across your healthcare workspace today.${customerId ? ` Customer ID: ${customerId}` : ''}`}</p></div><div className="dashboard-header__actions"><span className="dashboard-user">{member ? member.member_id : profile?.user_metadata?.name?.trim() || 'Customer'}</span><button className="dashboard-signout" type="button" onClick={handleSignOut} disabled={signingOut}><SafeIcon icon={FiLogOut}/>{signingOut ? 'Signing out' : 'Sign out'}</button></div></header>{renderActiveSection()}<div className="dashboard-section-footer"><Footer /></div></section></main> );}export default Dashboard;